import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ap2n-test-ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
